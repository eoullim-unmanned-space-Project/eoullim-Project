<link rel="stylesheet" href="/css/qna/qna.css">
<link rel="stylesheet" href="/css/layout/header.css"/>
{{> layout/header}}
<div class="qna-container">
    <div class="qna-card">
        <div class="qna-meta">
            <b>작성자</b>: {{qna.name}}
            <b>조회수</b>: {{qna.viewCount}}
        </div>

        <div class="qna-title">
            <h2><b>{{qna.title}}</b></h2>
        </div>

        <div class="qna-content">
            {{qna.content}}
        </div>

        {{#commentList}}
            <div class="comment-box">
                <div class="comment-item">
                    <div class="comment-header">
                        <div>{{name}}</div>
                        <small>{{createdAt}}</small>

                        {{#isEditing}}
                            <div class="comment-content">
                            {{#isOwner}}
                                <form class="qna-form" action="/comments/{{id}}/update" method="post">
                                    <textarea name="content" autofocus>{{content}}</textarea>
                                    <div class="qna-btn-group" style="justify-content: flex-end">
                                    <button class="qna-btn qna-btn-primary" type="submit" style="justify-content: flex-end">수정 완료</button>
                                    </div>
                                </form>
                            {{/isOwner}}
                            </div>
                        {{/isEditing}}

                        {{^isEditing}}
                            <div class="comment-content">
                                {{content}}


                            {{#isOwner}}
                                <div class="qna-btn-group" style="justify-content: flex-end">
                                    <form action="/comments/{{id}}/update" method="get">
                                        <button class="qna-btn qna-btn-secondary" type="submit">수정</button>
                                    </form>
                                    <form action="/comments/{{id}}/delete" method="post">
                                        <button class="qna-btn qna-btn-danger" type="submit">삭제</button>
                                    </form>
                                </div>
                            {{/isOwner}}
                            </div>
                        {{/isEditing}}
                    </div>
                </div>
            </div>
        {{/commentList}}

        {{^commentList}}
            <div class="qna-card">
                작성된 댓글이 없습니다.
            </div>
        {{/commentList}}
    </div>
</div>
{{> layout/footer}}